## Golden Record Principle

The `companies` table represents the canonical (golden) record for an organisation.

Raw data, external sources and AI-generated information are stored separately.

No AI process may overwrite verified company data directly.

Every fact should be traceable to one or more source records.
